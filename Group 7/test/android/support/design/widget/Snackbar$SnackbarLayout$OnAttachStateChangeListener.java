package android.support.design.widget;

import android.view.View;

interface Snackbar$SnackbarLayout$OnAttachStateChangeListener {
   void onViewAttachedToWindow(View var1);

   void onViewDetachedFromWindow(View var1);
}
