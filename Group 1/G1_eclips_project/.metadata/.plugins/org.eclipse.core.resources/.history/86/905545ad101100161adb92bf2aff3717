package com.se702.hider.enigma;

import android.graphics.Bitmap;

public class lllllllIII
{
    public lllllllIII() {
        super();
    }
    
    public static void lllllll(final EncoderService encoderService, final Bitmap bitmap, final String s) {
        if (encoderService.$496 == encoderService.llllllllI.length()) {
            lIllIllIIl.l(encoderService, bitmap, s);
        }
        llIIIIIIll.l(encoderService, bitmap, s);
    }
}
