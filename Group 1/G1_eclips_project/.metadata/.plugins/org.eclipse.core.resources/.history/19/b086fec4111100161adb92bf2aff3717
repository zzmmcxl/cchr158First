package com.se702.hider.enigma;

import android.graphics.Bitmap;

public class llllllIIIl
{
    public llllllIIIl() {
        super();
    }
    
    public static void llllll(final EncoderService encoderService, final Bitmap bitmap, final String s) {
        while (encoderService.$496 != encoderService.$510.length() || encoderService.$279 != s.length()) {
            encoderService.$301 = encoderService.$73.getNextRandomPoint();
            lllllllIII.lllllll(encoderService, bitmap, s);
        }
        IIlIIlllII.l(encoderService, bitmap, s);
    }
}
