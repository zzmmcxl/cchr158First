package com.se702.hider.enigma;

import android.content.Intent;
import android.graphics.Bitmap;

public class $531
{
    public $531() {
        super();
    }
    
    public static void l(final EncoderService encoderService, final Bitmap bitmap, final String s) {
        encoderService.$474 = new Intent("android.intent.action.MEDIA_SCANNER_SCAN_FILE");
        $819.$1(encoderService, bitmap, s);
    }
}
