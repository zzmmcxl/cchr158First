package com.se702.hider.enigma;

import android.graphics.Bitmap;

public class lllllllIII
{
    public lllllllIII() {
        super();
    }
    
    public static void lllllll(final EncoderService encoderService, final Bitmap bitmap, final String s) {
        if (encoderService.$496 == encoderService.$510.length()) {
            lIllIllIIl.$1(encoderService, bitmap, s);
        }
        llIIIIIIll.$1(encoderService, bitmap, s);
    }
}
