package com.se702.hider.enigma;

import android.content.Intent;

public class $1023
{
    public $1023() {
        super();
    }
    
    public static void I(final EncoderService encoderService, final Intent intent) {
        if (intent.getStringExtra("imageURI") != null) {
            $255.II(encoderService, intent);
        }
        IIIIllIIlI.IIII(encoderService, intent);
    }
}
